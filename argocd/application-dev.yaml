apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: my-vehicle-dashboard-dev
  namespace: openshift-gitops
  labels:
    app.kubernetes.io/name: my-vehicle-dashboard
    environment: development
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/kmotojim-rhdh-practice/my-vehicle-dashboard-manifests.git
    targetRevision: main
    path: overlays/dev
  destination:
    server: https://kubernetes.default.svc
    namespace: my-vehicle-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  revisionHistoryLimit: 10
